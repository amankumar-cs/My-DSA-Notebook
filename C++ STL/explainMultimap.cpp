#include<iostream>
using namespace std;
int main(){
    // everything same as map, only it can store multiple keys
    // only mpp[key] cannot be used here. becoz, {1,2} and {1,3} both may be there

    return 0;
}